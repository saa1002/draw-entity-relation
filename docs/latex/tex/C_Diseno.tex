\apendice{Especificación de diseño}

\section{Introducción}

En este anexo se describe el diseño general de la aplicación resultante de la evolución realizada durante el proyecto. El objetivo no es recoger todos los detalles internos del código, sino explicar cómo se organiza el editor y cómo se relacionan sus partes principales.

La aplicación parte de un proyecto heredado, por lo que el diseño final es el resultado de comprender una base existente y adaptarla progresivamente. El trabajo no consistió únicamente en añadir elementos visuales al lienzo, sino en mantener la coherencia entre la representación gráfica, la representación interna del diagrama, la validación, la transformación relacional y la generación de SQL.

De forma general, el sistema puede entenderse como una aplicación web compuesta por tres bloques principales. El primero es la interfaz visual, que permite al usuario crear y modificar el diagrama. El segundo es la representación interna o metamodelo, que almacena la información estructurada del diagrama. El tercero está formado por los procesos que trabajan sobre esa representación: validación, transformación relacional, generación de SQL, persistencia, importación y exportación.

La aplicación no incorpora una base de datos propia ni un esquema relacional persistente asociado al funcionamiento del sistema. El esquema relacional aparece como resultado generado a partir del diagrama E/R, siguiendo las reglas de transformación estudiadas en la teoría de bases de datos \cite{tema6}. Por este motivo, el diseño de datos no describe tablas internas de almacenamiento, sino el metamodelo utilizado por el editor para representar los diagramas E/R creados por el usuario y su correspondencia con el formato JSON exportado e importado.

\section{Diseño de datos}

El diseño de datos de la aplicación se centra en el metamodelo utilizado para representar los diagramas Entidad-Relación creados por el usuario. Los elementos contemplados por este metamodelo se apoyan en los conceptos del modelo E/R, como entidades, atributos, interrelaciones, cardinalidades, relaciones de grado superior y jerarquías ISA \cite{tema6}. La herramienta no utiliza una base de datos propia ni un servidor que almacene los diagramas, pero sí mantiene una representación estructurada del contenido del lienzo. Esta representación es la base sobre la que se apoyan la edición, la validación, la persistencia local, la importación y exportación en JSON, la transformación al modelo relacional y la generación de SQL.

En este contexto, se utiliza el término metamodelo porque la aplicación no modela directamente un dominio concreto, como una empresa, una biblioteca o un sistema de ventas, sino los elementos que pueden aparecer dentro de un diagrama E/R. Es decir, el sistema almacena información sobre entidades, atributos, relaciones, participantes, cardinalidades y jerarquías ISA. A partir de esa información puede reconstruir el diagrama visual y aplicar las operaciones propias del editor.

\subsection{Metamodelo del diagrama}

La Figura \ref{fig:metamodelo_er_diagrama} muestra una representación conceptual del metamodelo utilizado por la aplicación. Este diagrama no representa una base de datos física, sino la estructura lógica que permite describir los diagramas creados en el editor y conservar la información necesaria para procesarlos posteriormente.

\imagen{metamodelo_er_diagrama}{Metamodelo E/R de la representación interna de un diagrama.}

El elemento principal del metamodelo es el diagrama, que actúa como contenedor de los elementos creados por el usuario. Dentro de un diagrama pueden existir entidades, relaciones y jerarquías ISA. Las entidades representan los conjuntos de entidades del modelo E/R creado por el usuario, mientras que las relaciones representan las interrelaciones existentes entre ellas.

Las relaciones no enlazan directamente todos sus datos con las entidades, sino que incorporan participantes. Cada participante vincula una relación con una entidad concreta y conserva información propia de esa participación, como la cardinalidad mínima, la cardinalidad máxima y, cuando procede, el rol. Esta separación resulta especialmente útil para representar relaciones ternarias y casos en los que una misma entidad participa más de una vez en una relación con distintos roles, situaciones contempladas en el estudio de relaciones de grado superior y relaciones reflexivas \cite{tema6}. La comprobación de que una relación dispone de los participantes necesarios no se expresa únicamente mediante la cardinalidad del metamodelo, sino también mediante las reglas de validación del editor.

Los atributos se tratan como elementos propios del metamodelo porque pueden presentar distintas propiedades relevantes para la validación y la transformación posterior. Entre ellas se encuentran su condición de clave, atributo compuesto, atributo multivaluado o discriminante, según el tipo de elemento y el alcance soportado por la aplicación. Estas propiedades permiten reflejar parte de la semántica propia de los atributos del modelo E/R \cite{tema6}.

En el metamodelo, un atributo puede estar asociado a una entidad, a una relación o a otro atributo cuando forma parte de una estructura compuesta. Estas asociaciones representan alternativas de pertenencia dentro del editor, no la obligación de que un mismo atributo aparezca simultáneamente en todos esos contextos.

Las jerarquías ISA se representan mediante una entidad general y un conjunto de entidades especializadas, siguiendo la interpretación de las relaciones de generalización/especialización del modelo E/R \cite{tema6}. El alcance implementado contempla una generalización con una o varias especializaciones y clave heredada desde la entidad general. No se incluyen en el metamodelo implementado características como totalidad o parcialidad, solapamiento o exclusividad, discriminantes ISA ni herencia múltiple real, que quedan fuera del alcance desarrollado.

\subsection{Correspondencia con la representación JSON}

La representación JSON exportada por la aplicación sigue la organización definida por el metamodelo. Este formato se utiliza como sintaxis de intercambio de datos estructurados \cite{ecma-json}. El archivo generado almacena las colecciones de entidades, relaciones, atributos, jerarquías ISA y referencias necesarias para reconstruir el diagrama. Por tanto, el JSON no debe entenderse como una base de datos independiente, sino como una serialización del estado del editor.

Cada elemento conserva identificadores visuales que permiten relacionar la información lógica con su representación en el lienzo gestionado mediante mxGraph \cite{mxgraph}. Esta correspondencia es necesaria para reconstruir el diagrama tras una importación o después de recuperar el estado guardado en el almacenamiento local del navegador. De esta forma, la aplicación puede volver a crear las celdas visuales, las conexiones y los elementos auxiliares manteniendo la coherencia entre el metamodelo del diagrama y su representación gráfica.

La Figura \ref{fig:estructura_modelo_interno} resume esta correspondencia entre el metamodelo, la representación JSON y los procesos que trabajan sobre ella.

\imagen{estructura_modelo_interno}{Correspondencia simplificada entre el metamodelo, la representación JSON y los procesos de la aplicación.}

La persistencia local y la exportación a JSON reutilizan esta misma estructura. En el caso de la persistencia local, el almacenamiento se apoya en mecanismos del navegador asociados a la Web Storage API \cite{mdn-web-storage}. Cuando el usuario exporta un diagrama, la aplicación genera un archivo que contiene la información necesaria para volver a cargarlo. Cuando importa un archivo, el sistema interpreta esa estructura y reconstruye tanto el contenido lógico como los elementos visuales asociados. En las operaciones de combinación, el contenido importado se inserta de forma controlada para evitar conflictos directos de identificadores y facilitar la revisión posterior en el lienzo.

\subsection{Diccionario de datos}

A continuación se recoge un diccionario de datos simplificado del metamodelo. El objetivo no es enumerar todos los campos internos de implementación, sino describir el significado de los principales elementos almacenados por la aplicación y su papel dentro del editor.

\begin{table}[H]
\centering
\begin{tabular}{p{0.24\textwidth} p{0.66\textwidth}}
\textbf{Elemento} & \textbf{Descripción} \\
\hline
Diagrama & Contenedor principal del estado del editor. Agrupa las entidades, relaciones, atributos, jerarquías ISA y referencias necesarias para reconstruir el contenido del lienzo. \\

Entidad & Representa un conjunto de entidades del modelo E/R creado por el usuario. Conserva su nombre, su identificador visual y propiedades relevantes para la edición, como su posible condición de entidad débil, generalización o especialización ISA. \\

Relación & Representa una interrelación entre entidades. Conserva su nombre, identificador visual, grado, participantes, cardinalidades y propiedades específicas, como su posible condición de relación identificadora. \\

Participante & Elemento que vincula una relación con una entidad concreta. Permite conservar la cardinalidad mínima, la cardinalidad máxima y, en relaciones con entidades repetidas, el rol que diferencia cada participación. \\

Atributo & Representa una propiedad asociada a una entidad, a una relación o a otro atributo en el caso de atributos compuestos. Puede contener información sobre clave, discriminante, atributos compuestos, atributos multivaluados y subatributos. \\

Jerarquía ISA & Representa una estructura de generalización/especialización. Conserva la entidad general y las entidades especializadas, manteniendo la restricción de clave heredada dentro del alcance implementado. \\

Identificador visual & Referencia que permite mantener la correspondencia entre los elementos del metamodelo y las celdas del lienzo gestionadas mediante mxGraph. Es necesaria para reconstruir el diagrama y sincronizar los cambios visuales con la representación interna. \\
\end{tabular}
\caption{Diccionario de datos simplificado del metamodelo del diagrama.}
\label{tab:diccionario_metamodelo}
\end{table}

Este diseño permite separar, al menos conceptualmente, el contenido lógico del diagrama y su representación visual. No obstante, al tratarse de una aplicación heredada basada en mxGraph, existe un acoplamiento práctico entre ambos niveles durante la sincronización de elementos. Por esta razón, parte del trabajo realizado se orientó a mantener la coherencia entre el lienzo, la representación interna, la validación, la transformación relacional y la generación de SQL sin introducir una reescritura completa del editor.

\section{Diseño arquitectónico}

El diseño arquitectónico describe la organización general de la aplicación y la separación entre los bloques que intervienen en la edición, validación, transformación y persistencia de los diagramas. La herramienta se desarrolla como una aplicación web de cliente basada en React \cite{react}, por lo que la mayor parte de la lógica se ejecuta en el navegador del usuario.

La aplicación no dispone de un servidor propio encargado de procesar los diagramas ni de una base de datos remota asociada al proyecto. El despliegue en Vercel se utiliza para servir la aplicación web generada durante el proceso de construcción. Una vez cargada en el navegador, la herramienta trabaja localmente con el lienzo de mxGraph, la representación interna del diagrama, el almacenamiento local del navegador y los archivos importados o exportados por el usuario.

\subsection{Diagrama de despliegue}

La Figura \ref{fig:diagrama_despliegue_aplicacion} muestra el despliegue general de la aplicación. El usuario accede desde un navegador web a la versión publicada de UBU E-R App. Vercel \cite{vercel} sirve los recursos estáticos generados a partir de la aplicación React, pero no ejecuta la lógica de validación ni la transformación de los diagramas en un servidor propio. Estas operaciones se realizan en el cliente.

\imagen{diagrama_despliegue_aplicacion}{Diagrama UML de despliegue de la aplicación.}

En este despliegue pueden distinguirse tres elementos principales. En primer lugar, el repositorio GitHub, utilizado como sistema de control de versiones y como origen del código fuente. En segundo lugar, Vercel, que construye y publica la aplicación web a partir del repositorio. En tercer lugar, el navegador del usuario, donde se ejecuta la aplicación, se manipula el lienzo y se almacena temporalmente el estado del diagrama mediante \textit{LocalStorage}.

La importación y exportación de diagramas se realiza mediante archivos JSON gestionados desde el propio navegador. Del mismo modo, la exportación visual genera archivos de imagen a partir del contenido del lienzo. Por tanto, estas operaciones no requieren una base de datos externa ni un servicio backend específico.

\subsection{Diagrama de componentes}

La Figura \ref{fig:diagrama_componentes_aplicacion} resume los principales componentes lógicos de la aplicación y sus dependencias. El componente central es el editor de diagramas, que coordina la interfaz React, el lienzo gestionado mediante mxGraph, la representación interna del diagrama y los módulos de dominio.

\imagen{diagrama_componentes_aplicacion}{Diagrama UML de componentes de la aplicación.}

La interfaz de usuario se organiza alrededor del componente principal del editor y de los paneles de acciones. Estos paneles permiten crear elementos E/R, ejecutar operaciones globales sobre el diagrama y mostrar acciones contextuales en función de la selección actual. La interacción visual con el lienzo se apoya en mxGraph, que proporciona la infraestructura necesaria para representar y manipular celdas, conexiones y elementos gráficos.

La lógica del editor se apoya en varios grupos de módulos. Los \textit{hooks} del editor agrupan acciones relacionadas con historial, persistencia, atributos, relaciones, ISA y borrado. Las utilidades específicas del editor encapsulan operaciones de integración con mxGraph, renderizado, sincronización, selección, persistencia y reconstrucción visual. Por su parte, los módulos de dominio del modelo E/R concentran reglas y funciones independientes de la interfaz siempre que es posible.

Sobre la representación interna del diagrama trabajan los módulos de validación, transformación relacional y generación de SQL. La validación comprueba que el diagrama sea coherente dentro del alcance implementado. La transformación relacional construye una representación intermedia basada en tablas, columnas, claves primarias y claves foráneas. Finalmente, el servicio de generación de SQL convierte esa representación en un script compatible con el enfoque simplificado adoptado por la aplicación.

\subsection{Organización por módulos}

Además del diagrama de componentes, la Figura~\ref{fig:diagrama_paquetes_aplicacion} muestra una visión simplificada de la organización por módulos del código fuente. No se trata de paquetes en el sentido estricto de una aplicación Java, sino de una agrupación funcional de carpetas y módulos JavaScript que separan responsabilidades dentro de la aplicación React.

\imagen{diagrama_paquetes_aplicacion}{Diagrama UML de paquetes simplificado de la organización interna de la aplicación.}

El componente \texttt{DiagramEditor.js} actúa como orquestador principal del editor. Desde este componente se integran la inicialización del lienzo de mxGraph, las referencias principales del editor, los efectos de ciclo de vida y la composición de los módulos extraídos. Por este motivo, mantiene dependencias hacia los hooks del editor, los paneles de interfaz, las utilidades de integración con mxGraph y los módulos de dominio necesarios para validar y transformar el diagrama.

Los módulos agrupados en \texttt{hooks/} concentran acciones ligadas al comportamiento del editor, como la gestión del historial, la persistencia, las acciones sobre atributos, relaciones, jerarquías ISA y borrado. Estos hooks dependen del estado del editor y de las utilidades necesarias para aplicar cambios sobre el modelo y el lienzo, pero evitan que toda la lógica quede concentrada en el componente principal.

Los módulos de \texttt{panels/} contienen la interfaz lateral y los controles visibles para el usuario. Su responsabilidad principal es presentar acciones globales y contextuales, delegando la lógica de negocio en los hooks o en las funciones recibidas desde el componente principal. Esta separación permite modificar la disposición de la interfaz sin alterar directamente las reglas del modelo E/R.

Las utilidades del editor se agrupan según su responsabilidad principal. Las utilidades de integración con mxGraph encapsulan operaciones relacionadas con el grafo visual; las de renderizado se encargan de crear o reconstruir elementos gráficos; las de sincronización mantienen la correspondencia entre el lienzo y la representación interna; y las de persistencia gestionan operaciones de importación, exportación y almacenamiento local.

Los módulos de \texttt{domain/er} agrupan reglas y funciones propias del modelo Entidad-Relación, como entidades, relaciones, atributos, jerarquías ISA, composición de diagramas y validación. Estos módulos deberían mantenerse lo más independientes posible de la interfaz visual. Por su parte, \texttt{domain/relational} contiene la transformación desde el metamodelo E/R hacia una representación relacional basada en tablas, columnas, claves primarias y claves foráneas.

Finalmente, los servicios de SQL se encargan de convertir la representación relacional en un script textual. Esta separación evita mezclar la decisión conceptual de qué tablas deben generarse con la forma concreta en la que dichas tablas se escriben en SQL. La dependencia con mxGraph queda concentrada principalmente en el editor y en las utilidades visuales, mientras que los módulos de dominio y transformación relacional se mantienen más cercanos a la lógica del modelo.

\subsection{Mantenibilidad y extensibilidad}

La organización arquitectónica descrita no pretende eliminar por completo el acoplamiento existente en la aplicación heredada, sino reducirlo de forma progresiva allí donde aporta claridad y menor riesgo de mantenimiento. Por este motivo, \texttt{DiagramEditor.js} conserva el papel de orquestador principal, mientras que las acciones específicas, los paneles de interfaz, las utilidades de integración con mxGraph, los módulos de dominio y los servicios de generación SQL se separan en módulos diferenciados.

Esta separación facilita localizar el punto de cambio ante futuras ampliaciones. Las reglas propias del modelo Entidad-Relación deberían incorporarse en \texttt{domain/er}; las transformaciones hacia el modelo relacional, en \texttt{domain/relational}; la generación textual del script, en los servicios SQL; las acciones de edición, en los \textit{hooks} del editor; y los cambios de presentación, en los paneles o utilidades de renderizado correspondientes.

Las dependencias entre módulos se han mantenido en un nivel limitado y orientado al flujo natural de la aplicación. La interfaz y el editor dependen de los módulos de edición y de integración visual; el dominio E/R alimenta la transformación relacional; y la representación relacional se utiliza como entrada para la generación SQL. La dependencia con mxGraph queda concentrada principalmente en el editor y en las utilidades visuales, evitando extenderla innecesariamente a los módulos de dominio.

Aun así, sigue existiendo una dependencia práctica entre la representación visual y la representación interna durante la sincronización de elementos. Esta limitación es coherente con el origen heredado del proyecto y con la decisión de no realizar una reescritura completa del editor. Por ello, cualquier ampliación futura debería mantener la coherencia entre el lienzo, el metamodelo, la validación, la transformación relacional, la persistencia y la generación de SQL.

\section{Diseño procedimental}

El diseño procedimental recoge los flujos de funcionamiento más relevantes de la aplicación. No se pretende describir cada interacción posible de la interfaz, sino mostrar cómo las acciones principales del usuario se traducen en cambios sobre la representación interna del diagrama y cómo dicha representación se valida y transforma para generar resultados.

\subsection{Flujo general de edición}

El flujo principal comienza cuando el usuario crea o modifica elementos en el lienzo del editor. Cada acción visual debe reflejarse en la representación interna, de forma que lo que aparece en pantalla coincida con la información que la aplicación conserva y procesa.

Este flujo incluye operaciones como crear entidades, añadir atributos, configurar relaciones, establecer cardinalidades, mover elementos, renombrarlos o eliminarlos. En todos los casos, la aplicación debe mantener la sincronización entre la vista y la representación interna del diagrama.

Además, la interfaz contempla la selección múltiple de elementos. Este comportamiento permite mover o eliminar varios elementos de forma conjunta, pero evita mostrar acciones individuales cuando la selección contiene más de un elemento. De esta forma, se mantiene la coherencia entre las acciones disponibles en la interfaz y las operaciones que realmente pueden aplicarse sobre el modelo.

\subsection{Validación del modelo}

Antes de generar una salida relacional o SQL, la aplicación valida la representación interna del diagrama. Esta validación comprueba que el diagrama contiene la información necesaria y que no existen configuraciones incompletas o incompatibles con el alcance implementado.

La validación se ejecuta en operaciones como la generación de SQL y también puede lanzarse de forma explícita mediante la acción de comprobar el diagrama. Cuando se detectan errores, la aplicación debe mostrarlos de forma comprensible para que el usuario pueda corregir el modelo.

En el caso de ISA, la validación impide configuraciones fuera del alcance implementado, como una ISA sin generalización, una ISA sin especializaciones, especializaciones con clave propia o entidades especializadas en más de una jerarquía.

\subsection{Transformación al modelo relacional}

Si el diagrama supera la validación, la aplicación transforma el modelo Entidad-Relación a una representación relacional. Esta representación se basa en tablas, columnas, claves primarias y claves foráneas.

La existencia de una representación intermedia permite separar la lógica de transformación de la generación concreta del script SQL. De esta forma, la aplicación primero decide qué tablas y restricciones deben existir, y después construye el texto SQL correspondiente.

\subsection{Generación de SQL y ordenación de claves foráneas}

Una vez obtenida la representación relacional, la aplicación genera un script SQL a partir de las tablas, columnas, claves primarias y claves foráneas calculadas durante la transformación. Esta fase no vuelve a interpretar directamente el diagrama E/R, sino que trabaja sobre el modelo relacional intermedio generado en la fase anterior.

Uno de los aspectos más relevantes de esta generación es el tratamiento de las claves foráneas. Para que el script resultante sea más legible y pueda ejecutarse de forma ordenada, el generador intenta crear primero las tablas de las que dependen otras tablas. De esta forma, muchas claves foráneas pueden incluirse directamente dentro de la sentencia \texttt{CREATE TABLE} correspondiente.

El procedimiento parte del conjunto de tablas generadas y calcula, para cada una de ellas, los grupos de claves foráneas asociados. A continuación, mantiene un conjunto de tablas ya creadas y selecciona en cada iteración una tabla cuyas claves foráneas no dependan de tablas pendientes de creación. Si existe una tabla que cumple esta condición, se añade al orden de creación. Si no existe, se considera que hay una dependencia bloqueante, por ejemplo una dependencia circular, y las claves foráneas que impiden avanzar se aplazan para generarse al final mediante sentencias \texttt{ALTER TABLE}.

Este enfoque permite mantener una salida SQL compacta en los casos habituales, incorporando las claves foráneas dentro de los \texttt{CREATE TABLE}, pero conserva un mecanismo de respaldo para los casos en los que no es posible resolver todas las dependencias únicamente mediante reordenación de tablas.

El pseudocódigo simplificado del algoritmo es el siguiente:

\begin{verbatim}
entrada: lista de tablas del modelo relacional

tablasPendientes <- copia de la lista de tablas
tablasOrdenadas <- []
tablasCreadas <- conjunto vacío
clavesForaneasAplazadas <- []

mientras tablasPendientes no esté vacía:
buscar una tabla T en tablasPendientes tal que:
todas sus claves foráneas apunten a tablas ya creadas,
a la propia tabla,
o a tablas externas al conjunto generado

si existe T:
    mover T de tablasPendientes a tablasOrdenadas
    añadir nombre(T) a tablasCreadas

si no existe T:
    seleccionar una tabla pendiente
    identificar las claves foráneas que bloquean su creación
    añadir esas claves a clavesForaneasAplazadas
    mover la tabla seleccionada a tablasOrdenadas
    añadir su nombre a tablasCreadas

para cada tabla en tablasOrdenadas:
generar sentencia CREATE TABLE

para cada clave foránea aplazada:
generar sentencia ALTER TABLE
\end{verbatim}

La Figura~\ref{fig:actividad_ordenacion_sql} representa este proceso mediante un diagrama de actividad UML. En ella se observa que el algoritmo intenta resolver primero las dependencias mediante ordenación de tablas y solo recurre a claves foráneas aplazadas cuando no puede seleccionar ninguna tabla libre de dependencias bloqueantes.

\imagen{actividad_ordenacion_sql}{Diagrama de actividad UML del algoritmo de ordenación de tablas y claves foráneas.}

En la versión final de la aplicación, la salida SQL se mantiene deliberadamente sencilla. Se omiten nombres explícitos de restricciones de clave foránea cuando no resultan necesarios y se simplifican las referencias cuando una clave foránea apunta a la clave primaria completa de la tabla destino. Esta decisión afecta al renderizado textual del SQL, no a la transformación relacional previa.

La salida SQL debe entenderse como una traducción razonable del modelo relacional generado por la aplicación, no como un generador industrial completo para cualquier sistema gestor de bases de datos. Su objetivo principal dentro del proyecto es servir como apoyo académico y como punto de partida para comprender la transformación desde el modelo Entidad-Relación.

\subsection{Importación, combinación y generación de estructuras}

Además del flujo principal de edición, la aplicación incluye operaciones para importar diagramas JSON y generar estructuras básicas. En ambos casos, el usuario puede reemplazar el diagrama actual o combinar el nuevo contenido con el existente.

Cuando se combina contenido, la aplicación debe evitar conflictos directos de identificadores y mantener la coherencia de las referencias internas. Además, los elementos añadidos se insertan de forma controlada para facilitar su revisión visual dentro del lienzo.

Estas operaciones se diseñan como ayudas a la edición, no como mecanismos de fusión semántica avanzada. Por tanto, la aplicación no intenta interpretar si dos entidades representan el mismo concepto, sino insertar el contenido de forma controlada para que el usuario pueda revisarlo y ajustarlo posteriormente.

\section{Diseño de interfaces}

El diseño de la interfaz se centra en facilitar la edición de diagramas Entidad-Relación dentro de un lienzo web. La aplicación no se plantea como una herramienta de dibujo genérica, sino como un editor orientado a elementos concretos del modelo E/R. Por este motivo, la interfaz combina un lienzo de edición, una paleta lateral de elementos, acciones generales del diagrama, acciones contextuales dependientes de la selección y diálogos de configuración para aquellas operaciones que requieren introducir varios datos.

No se conservan bocetos iniciales independientes de la interfaz, ya que el diseño evolucionó de forma incremental sobre una aplicación heredada. Por ello, en esta sección se utilizan bocetos esquemáticos elaborados a partir de la versión final del editor. Estos bocetos no representan una propuesta futura ni una fase previa completa de diseño gráfico, sino una documentación simplificada de las principales decisiones de interfaz aplicadas durante el proyecto.

\subsection{Estructura general de la interfaz}

La interfaz se organiza en torno a dos zonas principales: el lienzo de edición y el panel lateral. El lienzo ocupa la parte central de la pantalla y permite crear, seleccionar, mover y conectar los elementos del diagrama. El panel lateral concentra la paleta de elementos E/R y las acciones disponibles para el usuario.

\imagen{boceto_interfaz_general}{Boceto de la estructura general de la interfaz del editor.}

La Figura~\ref{fig:boceto_interfaz_general} muestra esta organización general. En el lienzo se representa el contenido principal del editor mediante entidades, relaciones y atributos. En el panel lateral se agrupan las acciones de creación, las acciones contextuales asociadas a la selección actual y las acciones globales del diagrama. Además, la interfaz reserva una zona para mostrar información de versión o construcción de la aplicación.

Esta distribución permite mantener visible el diagrama mientras el usuario accede a las operaciones principales. También evita dispersar las acciones en menús independientes, lo que facilita el uso de la herramienta en un contexto académico y de aprendizaje del modelo E/R.

\subsection{Botonera general del diagrama}

La botonera general agrupa las acciones que afectan al conjunto del diagrama o al funcionamiento global de la aplicación. Entre ellas se incluyen la validación del diagrama, la generación de SQL, la importación y exportación JSON, la exportación visual, el ajuste de vista, la generación de estructuras predefinidas, el reinicio del diagrama y el acceso a información de ayuda.

\begin{figure}[H]
    \centering
    \includegraphics[
        height=0.78\textheight,
        keepaspectratio
    ]{img/boceto_botonera_general}
    \caption{Boceto de la botonera general del diagrama.}
    \label{fig:boceto_botonera_general}
\end{figure}

La Figura~\ref{fig:boceto_botonera_general} representa la organización de esta botonera. La parte superior del panel incluye el nombre de la aplicación y el selector de idioma. A continuación se sitúa la paleta de elementos E/R, desde la que el usuario puede crear elementos como entidades, relaciones o jerarquías ISA. Después se agrupan las acciones de historial, las acciones globales del diagrama y las opciones de información.

La separación por bloques facilita localizar las operaciones disponibles. Las acciones relacionadas con el diagrama se mantienen diferenciadas de la paleta de elementos y de las opciones informativas. De esta forma, el usuario puede distinguir entre crear nuevos elementos, modificar el estado global del diagrama o acceder a ayuda sobre la aplicación.

\subsection{Acciones contextuales}

Una decisión relevante del diseño de interfaz es el uso de acciones contextuales. Cuando el usuario selecciona un único elemento del diagrama, el panel lateral muestra operaciones específicas para ese tipo de elemento. En cambio, cuando no hay una selección aplicable o cuando la selección contiene varios elementos, se evita mostrar acciones individuales que podrían resultar ambiguas.

\imagen{boceto_acciones_contextuales}{Boceto de la botonera contextual para una entidad seleccionada.}

La Figura~\ref{fig:boceto_acciones_contextuales} muestra un ejemplo de entidad seleccionada. En este caso, el panel lateral identifica la selección actual y presenta acciones propias de una entidad, como añadir un atributo, esconder sus atributos, marcarla como entidad débil o eliminarla. La entidad seleccionada se resalta visualmente en el lienzo para indicar que las acciones mostradas se aplican sobre ella.

Este diseño reduce la cantidad de opciones visibles en cada momento y evita mostrar operaciones que no tienen sentido para la selección actual. Además, facilita la extensión futura de la interfaz, ya que nuevas acciones pueden asociarse al tipo de elemento correspondiente sin modificar la organización general del panel.

\subsection{Diálogos de configuración}

Además de la botonera lateral, la aplicación utiliza diálogos para operaciones que requieren introducir o revisar varios datos antes de modificar el diagrama. Este enfoque se aplica, por ejemplo, en la configuración de relaciones, la asignación de cardinalidades, la importación de diagramas o la configuración de jerarquías ISA dentro del alcance implementado.

\imagen{boceto_dialogo_configuracion}{Boceto de los diálogos de configuración de relaciones y cardinalidades.}

La Figura~\ref{fig:boceto_dialogo_configuracion} muestra un boceto simplificado de un diálogo de configuración de relación. En este tipo de operación, el usuario debe revisar los participantes de la relación y establecer las cardinalidades correspondientes. Por ello, la información se presenta en una ventana separada del panel lateral, con controles específicos y acciones de confirmación o cancelación.

El uso de diálogos permite separar las operaciones simples, que pueden ejecutarse directamente desde el panel, de aquellas que requieren una configuración previa. En el caso de las relaciones, esta separación resulta especialmente útil porque la representación visual del lienzo debe mantenerse coherente con los participantes, las cardinalidades y la representación interna del diagrama.

\subsection{Criterios de diseño aplicados}

El diseño final de la interfaz sigue varios criterios prácticos. En primer lugar, se prioriza la visibilidad del lienzo, ya que el diagrama es el elemento central de trabajo. En segundo lugar, se separan la paleta de elementos, las acciones globales y las acciones contextuales, reduciendo la carga visual y evitando operaciones no aplicables. En tercer lugar, las operaciones que requieren varios parámetros se desplazan a diálogos específicos para que el usuario pueda revisar la configuración antes de confirmar los cambios.

El diseño visual mantiene la notación propia del modelo Entidad-Relación: entidades, relaciones, atributos, atributos clave, atributos compuestos, atributos multivaluados, entidades débiles, relaciones identificadoras e ISA se representan mediante convenciones gráficas diferenciadas. De esta forma, la interfaz busca que el usuario pueda reconocer los elementos del diagrama sin que las mejoras de usabilidad alteren su significado académico.

